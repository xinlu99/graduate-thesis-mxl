# 第三章 面向天基网络的协同计算卸载方法设计（完整整理）

## 3.1 概述与问题动机

### 3.1.1 去中心化环境下的自利行为困境

在绪论所描述的天基网络 "分布式试验管控体系" 中，大量异构计算节点（如卫星终端 ST、卫星边缘节点 SEN）并行处理着动态到达的各类任务。由于缺乏一个强有力的中心协调者，每个节点天然地倾向于做出局部最优的 "自利" 决策，这在资源竞争激烈的环境中极易导致 "单节点最优≠系统最优" 的困境。

具体而言，一个理性的自利节点在进行任务接纳或资源分配决策时，会优先抢占那些预计收益高、计算量小的任务，同时倾向于推卸或拒绝那些计算密集、优先级低的任务。当所有节点都遵循此策略时，高价值任务会迅速向少数几个性能指标（如宣称的低负载）看似最优的节点集中，导致这些节点成为热点（Hotspot）并陷入过载（Overload）状态。与此同时，那些被推卸的低优先级任务则可能在网络中被反复传递，迟迟得不到处理，而另一些非热点节点则可能处于资源闲置状态。

这种由自利行为引发的负载失衡（Load Imbalance），是导致系统整体性能退化的根源。如图 3.1 所示，在自利决策场景下，当节点 A 和节点 B 都试图将高收益任务卸载至当时看起来最优的节点 C 时，节点 C 的处理队列迅速饱和，新任务的等待时延与正在执行任务的并发开销（Concurrency Overhead）急剧增加。反之，对于低优先级的任务，节点 A 和 B 均选择拒绝，导致这些任务在网络中滞留。最终，系统的整体吞吐量下降、平均任务完成时延恶化，并出现任务处理的严重不公平现象，即 "公地悲剧"（Tragedy of the Commons）。而在协同决策场景下，通过联盟链提供的全局状态信息，节点能够做出更加理性的负载感知分配，实现系统均衡。

> 图 3.1 自私决策与协同决策对比示意（暂时无法在飞书文档外展示此内容）
> 
> 

### 3.1.2 问题的多维复杂性

更进一步，该问题在真实天基网络环境中因以下因素而显著加剧：
（1）节点异构性（Heterogeneity）
各节点的计算能力、存储容量、能耗特性各不相同，使得 "最优" 节点的评估标准更为复杂，加剧了决策偏差。我们将节点异构性形式化定义如下：
设节点集合  $\mathcal{N} = {1, 2, ..., N}$ ，每个节点  $i$  的异构特征向量为：
 $\mathbf{h}_i = (f_i^{max}, M_i, E_i^{cap}, \kappa_i, \eta_i)$ 
其中各参数定义如下：
 $f_i^{max}$ ：最大 CPU 频率（cycles/s），范围通常为  $[10^8, 10^{10}]$ 
 $M_i$ ：可用内存容量（GB），典型值为  $[2, 32]$ 
 $E_i^{cap}$ ：能量存储容量（J），取决于太阳能板充电效率
 $\kappa_i$ ：计算能效系数，定义为  $\kappa_i = P_{compute}/f_i^3$ 
 $\eta_i$ ：通信能效系数，定义为  $\eta_i = P_{tx}/R_{tx}$ 

节点异构性可通过变异系数（Coefficient of Variation）量化：
 $CV_h = \frac{\sigma(\mathbf{h})}{\mu(\mathbf{h})}$ 
当  $CV_h > 0.5$  时，认为系统具有高度异构性。

（2）任务多样性（Task Diversity）
轻量、中等、重型任务的混合到达，使得节点的资源消耗速率难以预测，自利决策更容易导致误判。任务到达过程可建模为非齐次泊松过程：
 $\lambda(t) = \lambda_0 \cdot \left(1 + \alpha \sin(\omega t + \phi)\right)$ 
其中  $\lambda_0$  为基础到达率， $\alpha \in [0, 1]$  为波动幅度， $\omega$  为周期频率（对应卫星轨道周期）， $\phi$  为相位偏移。

任务类型分布服从混合高斯分布，任务  $j$  的计算需求  $c_j$  满足：
 $c_j \sim \sum_{k=1}^{K} \pi_k \cdot \mathcal{LN}(\mu_k, \sigma_k^2)$ 
其中  $K=3$  对应轻量 / 中等 / 重型三类任务， $\mathcal{LN}$  为对数正态分布，混合权重  $\pi = (0.5, 0.35, 0.15)$  反映实际任务分布特征。

|任务类型|计算量均值|计算量标准差|数据大小均值|混合权重|
|---|---|---|---|---|
|轻量型| $5 \times 10^7$  cycles| $2 \times 10^7$ |2 MB|0.50|
|中等型| $2 \times 10^8$  cycles| $5 \times 10^7$ |8 MB|0.35|
|重型| $8 \times 10^8$  cycles| $2 \times 10^8$ |25 MB|0.15|
**表 3.1 任务类型参数分布**

（3）联盟链记账延迟（Ledger Latency）
即使引入区块链作为公共信息来源，其固有的共识与广播延迟也意味着节点获取的 "全局" 状态是滞后的，基于过时信息的决策可能进一步放大上述的资源抢占问题。记账延迟的完整模型为：
 $D_{chain} = D_{consensus} + D_{broadcast} + D_{local}$ 
各分量的详细建模：

- 共识延迟  $D_{consensus}$ ：采用 PBFT 共识时，需要经过 Pre-prepare、Prepare、Commit 三阶段
 $D_{consensus} = D_{pre-prepare} + D_{prepare} + D_{commit} \sim \mathcal{N}(\mu_c, \sigma_c^2)$ 
典型参数： $\mu_c = 200ms$ ， $\sigma_c = 50ms$ 

- 广播延迟  $D_{broadcast}$ ：与网络规模和拓扑相关
 $D_{broadcast} = \max_{i,j \in \mathcal{N}} d_{ij} + D_{propagation}$ 
其中  $d_{ij}$  为节点  $i$  到  $j$  的最短路径延迟

- 本地处理延迟  $D_{local}$ ：区块验证与存储时间
 $D_{local} \sim Exp(\lambda_l), \quad \lambda_l \approx 100ms$ 

（4）信誉演化（Reputation Evolution）
在缺乏有效监管时，节点甚至可能通过虚报自身负载来获取不正当利益。设节点  $i$  的真实负载为  $L_i^{true}$ ，声明负载为  $L_i^{declared}$ ，则欺骗程度定义为：
 $\delta_i = \frac{|L_i^{declared} - L_i^{true}|}{L_i^{true}}$ 
欺骗行为会随时间演化，形成 "军备竞赛" 式的恶性循环。本章通过智能合约实现的信誉机制来抑制此类行为。

### 3.1.3 本章研究目标与贡献

因此，本章的核心目标在于，在保持天基网络去中心化特性的前提下，构建一个能够有效缓解自利行为、促进系统级协同的资源博弈框架。我们并不追求在这一层面解决单个任务的卸载最优问题（该问题留至第 4 章），而是聚焦于如何通过多智能体强化学习（MARL）与区块链的深度融合，引导所有计算节点学习一套宏观的、能够保障系统稳定性（Stability）与公平性（Fairness）的资源分配策略。

本章的主要贡献可概括为以下四点：

1. 协同博弈理论建模：首次将天基网络多节点资源分配问题建模为带有公共知识约束的多智能体马尔可夫博弈（Multi-Agent Markov Game, MAMG），并从博弈论视角证明了混合策略纳什均衡的存在性条件，为算法设计提供了理论保障。

2. 区块链增强的 CTDE 框架：设计了一种新型的 "区块链即公共知识库" 的集中训练 - 分散执行（CTDE）训练范式。该框架利用链上数据作为中心化训练时的全局信息代理，创新性地解决了传统 CTDE 对中心化实时信息依赖的问题，实现了可信与效率的有效平衡。

3. 混合奖励机制设计：提出了融合自利效率、系统公平和策略稳定的三元混合奖励函数，并引入拉格朗日松弛方法实现了多目标间的自适应权衡，避免了 "过度追求公平而牺牲吞吐" 的陷阱。

4. 全面的实验验证：通过大规模仿真实验，从收敛性、有效性、鲁棒性、可扩展性四个维度全面验证了所提方法的优越性，并提供了详细的消融研究和敏感性分析。

## 3.2 系统层协同博弈建模

为将上述问题形式化为可通过 MARL 求解的形式，我们将天基网络中的多节点资源分配问题建模为一个多智能体马尔可夫博弈（Multi-Agent Markov Game, MAMG）。在该模型中，每个卫星边缘节点（SEN）被视为一个独立的智能体（Agent），它们在共享的环境中交互并学习协同策略。

### 3.2.1 多智能体马尔可夫博弈形式化

定义 3.1（多智能体马尔可夫博弈） 一个 N - 智能体马尔可夫博弈可由七元组定义：
 $\mathcal{G} = \langle \mathcal{N}, \mathcal{S}, {\mathcal{A}^i}_{i \in \mathcal{N}}, \mathcal{P}, {R^i}_{i \in \mathcal{N}}, \gamma, {\mathcal{O}^i}_{i \in \mathcal{N}} \rangle$ 
各元素定义如下：

|符号|定义|描述|
|---|---|---|
| $\mathcal{N}$ | ${1, 2, ..., N}$ |智能体（SEN 节点）集合|
| $\mathcal{S}$ |-|全局状态空间，包含所有节点的完整状态信息|
| $\mathcal{A}^i$ |-|智能体  $i$  的动作空间，资源分配比例或接纳意愿|
| $\mathcal{A}$ | $\prod_{i \in \mathcal{N}} \mathcal{A}^i$ |联合动作空间|
| $\mathcal{P}$ | $\mathcal{S} \times \mathcal{A} \times \mathcal{S} \rightarrow [0, 1]$ |状态转移概率函数|
| $R^i$ | $\mathcal{S} \times \mathcal{A} \times \mathcal{S} \rightarrow \mathbb{R}$ |智能体  $i$  的奖励函数|
| $\gamma$ | $\in [0, 1)$ |折扣因子|
| $\mathcal{O}^i$ | $\mathcal{S} \rightarrow \Omega^i$ |智能体  $i$  的观测函数|
**表 3.2 多智能体马尔可夫博弈元组定义**

定义 3.2（部分可观测性） 由于去中心化特性，每个智能体  $i$  仅能获得全局状态  $s \in \mathcal{S}$  的一个局部观测  $o^i = \mathcal{O}^i(s) \in \Omega^i$ ，其中观测空间  $\Omega^i \subseteq \mathcal{S}$ 。这种部分可观测性是本问题区别于标准多智能体博弈的关键特征。

### 3.2.2 智能体、状态、动作与奖励设计

（一）智能体（Agent）设计
系统中的  $N$  个计算节点（SENs）分别对应  $N$  个独立的智能体，即  ${Agent_1, Agent_2, ..., Agent_N}$ 。每个智能体  $i$  的目标是通过与其他智能体交互，学习一个策略：
 $\pi^i: \Omega^i \rightarrow \Delta(\mathcal{A}^i)$ 
其中  $\Delta(\mathcal{A}^i)$  表示动作空间  $\mathcal{A}^i$  上的概率分布集合。

智能体  $i$  的目标函数为最大化长期累积折扣回报：
 $J^i(\pi^i, \pi^{-i}) = \mathbb{E}_{\pi^i, \pi^{-i}} \left[ \sum_{t=0}^{\infty} \gamma^t r_t^i \mid s_0 \right]$ 
其中  $\pi^{-i} = (\pi^1, ..., \pi^{i-1}, \pi^{i+1}, ..., \pi^N)$  表示除智能体  $i$  外所有其他智能体的联合策略， $r_t^i$  为时刻  $t$  智能体  $i$  获得的即时奖励。

（二）状态（State）空间设计
在时刻  $t$ ，每个智能体  $i$  的观测状态  $o_t^i$  由两部分构成：
本地私有状态（Local State）  $l_t^i \in \mathbb{R}^{d_l}$ ，包含只有该节点自身实时知晓的信息：
 $l_t^i = \left( Q_t^i, \rho_{cpu}^i(t), \rho_{mem}^i(t), E_t^i, \mathbf{h}_i, \mathbf{w}_t^i \right)$ 

|分量|符号|维度|描述|
|---|---|---|---|
|任务队列特征| $Q_t^i$ | $K \times d_q$ |队列中  $K$  个任务的特征向量|
|CPU 利用率| $\rho_{cpu}^i(t)$ |1|瞬时 CPU 利用率， $\in [0, 1]$ |
|内存利用率| $\rho_{mem}^i(t)$ |1|瞬时内存利用率， $\in [0, 1]$ |
|剩余能量| $E_t^i$ |1|归一化剩余能量， $\in [0, 1]$ |
|异构参数| $\mathbf{h}_i$ |5|节点硬件特征向量|
|历史负载| $\mathbf{w}_t^i$ | $W$ |最近  $W$  步的负载序列|
**表 3.3 本地私有状态分量说明**

链上公共摘要（Public Summary）  $g_t \in \mathbb{R}^{d_g}$ ，是从联盟链账本中查询到的、带有一定延迟的全局信息：
 $g_t = \left( \bar{L}_t, \sigma_L^2(t), \mathbf{D}_t^{task}, \mathbf{R}_t^{rep}, \hat{D}_{chain}(t), \mathbf{T}_t^{stat} \right)$ 

|分量|计算方式|描述|
|---|---|---|
|平均负载| $\bar{L}_t = \frac{1}{N} \sum_{j=1}^{N} L_j(t-\tau)$ |近  $\tau$  步各节点的平均负载|
|负载方差| $\sigma_L^2(t) = \frac{1}{N} \sum_{j=1}^{N} (L_j - \bar{L})^2$ |负载分布的离散程度|
|任务事件分布| $\mathbf{D}_t^{task} = (n_{start}, n_{end}, \bar{d})$ |start/end 事件统计|
|信誉分向量| $\mathbf{R}_t^{rep} = (r_1^{rep}, ..., r_N^{rep})$ |各节点的信誉评分|
|延迟估计| $\hat{D}_{chain}(t)$ |对当前账本延迟的估计值|
|任务类型统计| $\mathbf{T}_t^{stat}$ |各类任务的到达频率|
**表 3.4 链上公共摘要分量说明**

因此，智能体  $i$  的完整观测为：
 $o_t^i = (l_t^i, g_t) \in \Omega^i = \mathbb{R}^{d_l + d_g}$ 

（三）动作（Action）空间设计
在时刻  $t$ ，智能体  $i$  的动作为  $a_t^i \in \mathcal{A}^i$ ，我们将其设计为连续的资源分配比例，这与 PPO 算法能有效处理连续动作空间的特性相匹配。

基础动作设计（接纳阈值 / 意愿）：
 $a_t^i = \sigma(\theta_t^i) \in (0, 1]$ 
其中  $\sigma(\cdot)$  为 sigmoid 函数， $\theta_t^i$  为策略网络输出的原始值。 $a_t^i$  表示节点  $i$  当前愿意接纳新任务的意愿强度：
 $a_t^i \rightarrow 1$ ：节点积极接纳任务
 $a_t^i \rightarrow 0$ ：节点倾向于拒绝任务

扩展动作设计（资源定价与准入联合决策）：
 $\mathbf{a}_t^i = \left( a_t^{i,accept}, a_t^{i,price} \right) \in (0, 1] \times [p_{min}, p_{max}]$ 
其中  $a_t^{i,accept}$  为接纳意愿， $a_t^{i,price}$  为服务定价因子。

任务分配机制：基于所有节点的接纳意愿，新任务分配至节点  $j$  的概率为：
 $P(task \rightarrow node_j) = \frac{\exp(a_t^j / T)}{\sum_{k=1}^{N} \exp(a_t^k / T)}$ 
其中  $T$  为温度参数：
 $T \rightarrow 0$ ：分配趋于确定性（选择最高意愿节点）
 $T \rightarrow \infty$ ：分配趋于均匀随机

（四）奖励（Reward）函数设计
为引导智能体学习兼顾个体利益与系统公平的策略，我们设计了一个多目标加权的混合奖励函数。

自利效率项（Self-Interest Efficiency）  $R_{self}^i$ ：
 $R_{self}^i = -\left( w_d \cdot \tilde{D}_t^i + w_e \cdot \tilde{E}_t^i + w_q \cdot \tilde{Q}_t^i \right)$ 
其中归一化指标定义为：
 $\tilde{D}_t^i = \frac{D_t^i - D_{min}}{D_{max} - D_{min}}, \quad \tilde{E}_t^i = \frac{E_t^i - E_{min}}{E_{max} - E_{min}}, \quad \tilde{Q}_t^i = \frac{|Q_t^i|}{Q_{max}}$ 

系统公平项（System Fairness）  $R_{fair}$ ：
我们引入三个互补的公平性度量指标：
Jain 公平指数：
 $J(\mathbf{L}) = \frac{\left( \sum_{i=1}^{N} L_i \right)^2}{N \cdot \sum_{i=1}^{N} L_i^2} \in \left[ \frac{1}{N}, 1 \right]$ 
该指数越接近 1 表示负载分布越均衡。特别地，当所有节点负载相等时  $J=1$ ，当负载完全集中于单一节点时  $J=1/N$ 。

负载方差（归一化）：
 $\widetilde{Var}(\mathbf{L}) = \frac{Var(\mathbf{L})}{Var_{max}} = \frac{\frac{1}{N} \sum_{i=1}^{N} (L_i - \bar{L})^2}{(\bar{L})^2}$ 

基尼系数：
 $G(\mathbf{L}) = \frac{\sum_{i=1}^{N} \sum_{j=1}^{N} |L_i - L_j|}{2N \sum_{i=1}^{N} L_i} \in [0, 1]$ 

综合公平项设计为：
 $R_{fair} = w_j \cdot J(\mathbf{L}) - w_v \cdot \widetilde{Var}(\mathbf{L}) - w_g \cdot G(\mathbf{L})$ 

稳定性惩罚项（Stability Penalty）  $R_{stab}$ ：
 $R_{stab} = -\beta \cdot \left| a_t^i - a_{t-1}^i \right|_2^2 - \eta \cdot \mathbb{I}\left[ \left| a_t^i - a_{t-1}^i \right| > \delta_{max} \right]$ 
其中：
 $\beta$ ：平滑惩罚系数，典型值 0.1
 $\eta$ ：跳变惩罚系数，典型值 0.5
 $\delta_{max}$ ：允许的最大动作变化阈值，典型值 0.3
 $\mathbb{I}[\cdot]$ ：指示函数

最终混合奖励函数：
 $r_t^i = w_s \cdot R_{self}^i + w_f \cdot R_{fair} + w_c \cdot R_{stab}$ 
其中权重满足归一化约束  $w_s + w_f + w_c = 1$ 。

|权重参数|符号|默认值|作用|
|---|---|---|---|
|自利权重| $w_s$ |0.3|控制个体效率优化强度|
|公平权重| $w_f$ |0.6|控制系统公平性追求强度|
|稳定权重| $w_c$ |0.1|控制策略平滑约束强度|
**表 3.5 奖励函数权重参数设置**

### 3.2.3 约束条件与拉格朗日正则化

在优化上述奖励函数时，可能会出现 "过度追求公平而牺牲整体吞吐" 的极端情况。为避免这种问题，我们引入以下显式约束：
约束 C1 - 吞吐量下界约束：
 $Throughput_{total}(t) \geq Throughput_{min}$ 
确保系统不会为了绝对公平而拒绝所有任务。

约束 C2 - 资源利用率范围约束：
 $\rho_{min} \leq \bar{\rho}(t) \leq \rho_{max}$ 
避免资源过度闲置或过度使用。典型参数： $\rho_{min}=0.3$ ， $\rho_{max}=0.9$ 。

约束 C3 - 服务质量约束：
 $P(D_t > D_{QoS}) \leq \epsilon_{QoS}$ 
控制时延违约的概率。典型参数： $D_{QoS}=10$  steps， $\epsilon_{QoS}=0.05$ 。

拉格朗日松弛方法：
将硬约束转化为软惩罚项，构造拉格朗日函数：
 $\mathcal{L}(\theta, \boldsymbol{\lambda}) = J(\theta) - \sum_{k=1}^{3} \lambda_k \cdot \max(0, g_k(\theta))$ 
其中  $\boldsymbol{\lambda} = (\lambda_1, \lambda_2, \lambda_3)$  为拉格朗日乘子向量， $g_k(\theta)$  为第  $k$  个约束的违反量。

乘子更新规则（对偶梯度上升）：
 $\lambda_k^{t+1} = \left[ \lambda_k^t + \alpha_\lambda \cdot g_k(\theta^t) \right]^+$ 
其中  $[\cdot]^+ = \max(0, \cdot)$  为投影算子， $\alpha_\lambda$  为乘子学习率。

### 3.2.4 纳什均衡理论分析

定理 3.1（纳什均衡存在性） 在所提出的多智能体马尔可夫博弈  $\mathcal{G}$  中，若满足以下条件：
(i) 状态空间  $\mathcal{S}$  和动作空间  $\mathcal{A}^i$  均为紧致凸集；
(ii) 奖励函数  $R^i(s, a^i, a^{-i})$  关于  $a^i$  连续且拟凹；
(iii) 状态转移概率  $\mathcal{P}(s'|s, a)$  关于  $(s, a, s')$  满足 Lipschitz 连续性；
则存在至少一个混合策略纳什均衡  $\pi^* = (\pi^{1*}, ..., \pi^{N*})$ 。

**证明：**
我们利用 Kakutani 不动点定理进行证明。
Step 1：定义最优响应（Best Response）映射。对于智能体  $i$ ，给定其他智能体的策略  $\pi^{-i}$ ，最优响应为：
 $BR^i(\pi^{-i}) = \arg\max_{\pi^i \in \Delta(\mathcal{A}^i)} J^i(\pi^i, \pi^{-i})$ 
Step 2：证明  $BR^i$  是非空凸值的上半连续集值映射。

- 非空性：由条件 (i)， $\Delta(\mathcal{A}^i)$  是紧致的；由条件 (ii)， $J^i$  是连续的。根据 Weierstrass 极值定理， $BR^i$  非空。

- 凸值性：由条件 (ii) 的拟凹性， $BR^i(\pi^{-i})$  是凸集。

- 上半连续性：由条件 (iii) 的 Lipschitz 连续性和条件 (ii) 的连续性，可证明  $BR^i$  是上半连续的。
Step 3：定义联合最优响应映射：
 $BR(\pi) = \prod_{i=1}^{N} BR^i(\pi^{-i})$ 
由于各  $BR^i$  满足 Kakutani 定理条件， $BR$  也满足。因此存在不动点  $\pi^* \in BR(\pi^*)$ ，即纳什均衡。  $\square$ 

定理 3.2（收敛性） 在上述博弈中，采用 MAPPO 算法的策略序列  ${\pi^{(k)}}_{k=1}^{\infty}$  以概率 1 收敛到纳什均衡的邻域。
该定理的证明依赖于随机逼近理论和 PPO 算法的收敛性保证，完整证明从略。

## 3.3 区块链 "公共博弈信息板" 强化设计

为支撑上述 MARL 模型，区块链在此不仅仅是数据存证的工具，更是构建多智能体公共知识（Common Knowledge）与博弈规则的核心基础设施，我们称之为 "公共博弈信息板"。本节将详细阐述其机制设计。

### 3.3.1 联盟链架构设计

我们设计一个面向天基网络的分层联盟链（Hierarchical Permissioned Blockchain）架构，以平衡性能、安全性和可扩展性需求。

|层次|功能|节点类型|共识机制|
|---|---|---|---|
|主链层|全局共识、信誉管理、策略参数同步|CC 节点、核心 SEN|PBFT|
|子链层|区域资源事件、任务事件记录|区域内 SEN、ST|简化 PBFT/Raft|
|节点层|本地数据采集、区块提交|所有 ST、SEN|-|
**表 3.6 分层联盟链架构设计**

> 图 3.2 分层联盟链架构示意（暂时无法在飞书文档外展示此内容）
> 
> 

### 3.3.2 区块类型与字段设计

为满足系统层博弈的需求，我们定义以下几类核心区块：

（1）资源块（Resource Block）
用于记录节点周期性上报的资源快照。

|字段名|类型|描述|
|---|---|---|
|BlockHeader|struct|区块头（版本、前块哈希、时间戳、Merkle 根）|
|Timestamp|uint64|Unix 时间戳|
|NodeID|bytes32|节点唯一标识|
|NodeHeteroProfile|struct|节点异构配置（算力、内存、能耗系数）|
|LoadMetrics|struct|负载指标（CPU 利用率、内存使用、队列长度）|
|ConcurrencyModelHash|bytes32|第 4 章并发开销模型的哈希，用于版本验证|
|ReputationScore|float64|当前信誉评分|
|LedgerLatency|uint32|估计的账本延迟（ms）|
|Signature|bytes|节点签名|
**表 3.7 资源块字段定义**

（2）任务开始块（Task-start Block）
记录任务被接纳并开始执行的事件。

|字段名|类型|描述|
|---|---|---|
|BlockHeader|struct|区块头|
|TaskID|bytes32|任务唯一标识|
|TaskType|enum|任务类型（轻量 / 中等 / 重型）|
|SourceNodeID|bytes32|任务来源节点（ST）|
|TargetNodeID|bytes32|任务执行节点（SEN）|
|SubmitTime|uint64|任务提交时间|
|RequiredResources|struct|资源需求（计算量、数据大小）|
|PredictedDelay|float64|预测执行时延|
|QueueSnapshot|struct|目标节点队列快照|
**表 3.8 任务开始块字段定义**

（3）任务结束块（Task-end Block）
记录任务执行完成或失败的事件。

|字段名|类型|描述|
|---|---|---|
|BlockHeader|struct|区块头|
|TaskID|bytes32|任务唯一标识（关联 Task-start）|
|EndTime|uint64|任务完成时间|
|ExecutionResult|enum|执行结果（成功 / 失败 / 超时）|
|ActualDelay|float64|实际执行时延|
|ActualEnergy|float64|实际能耗|
|QueueStateSnapshot|struct|执行时队列状态|
|ResultHash|bytes32|计算结果哈希（用于验证）|
**表 3.9 任务结束块字段定义**

（4）信誉更新块（Reputation-Update Block）
记录智能合约自动执行的信誉变更事件。

|字段名|类型|描述|
|---|---|---|
|BlockHeader|struct|区块头|
|NodeID|bytes32|被更新节点|
|PreviousScore|float64|更新前信誉分|
|NewScore|float64|更新后信誉分|
|Delta|float64|变化量（正 / 负）|
|Reason|string|变更原因（任务完成 / 虚报 / 超时）|
|TriggeringEventHash|bytes32|触发事件的区块哈希|
|EvidenceList|[]bytes32|证据区块列表|
|ValidatorSignatures|[]bytes|验证节点签名|
**表 3.10 信誉更新块字段定义**

（5）策略同步块（Policy-Sync Block）
用于 MARL 训练过程中策略参数的同步。

|字段名|类型|描述|
|---|---|---|
|BlockHeader|struct|区块头|
|PolicyVersion|uint64|策略版本号|
|ConsensusRound|uint64|当前共识轮次|
|AggregatedGradients|bytes|聚合后的梯度（压缩编码）|
|GlobalModelHash|bytes32|全局模型参数哈希|
|ParticipantList|[]bytes32|参与本轮更新的节点列表|
|PerformanceMetrics|struct|性能指标（Jain 指数、吞吐量等）|
**表 3.11 策略同步块字段定义**

### 3.3.3 智能合约与信誉机制设计

#### 信誉评分模型

节点  $i$  在时刻  $t$  的信誉分由以下递归公式更新：
 $Rep_i(t) = \alpha_{base} \cdot Rep_i(t-1) + \sum_{k} \alpha_k \cdot S_k^i(t)$ 
其中各评分项定义如下：

|评分项|符号|权重|计算方式|
|---|---|---|---|
|任务完成质量| $S_{task}^i$ |0.4|基于时延、成功率的综合评分|
|诚实行为评分| $S_{honest}^i$ |0.3|声明负载与实际负载一致性|
|协作贡献评分| $S_{coop}^i$ |0.2|对系统公平性的贡献度|
|违规惩罚| $S_{penalty}^i$ |-0.5|虚报、超时等违规行为|
**表 3.12 信誉评分权重分配**

任务完成质量评分：
 $S_{task}^i(t) = \omega_1 \cdot \left(1 - \frac{D_{actual} - D_{predicted}}{D_{max}}\right) + \omega_2 \cdot \mathbb{I}[success]$ 

诚实性评分：
定义负载声明偏差：
 $\delta_i^{load} = \frac{|L_i^{declared} - L_i^{actual}|}{L_i^{actual}}$ 
诚实性评分：
 $S_{honest}^i(t) = \begin{cases}1 - \delta_i^{load} & \text{if } \delta_i^{load} \leq \delta_{threshold} \-\kappa \cdot \delta_i^{load} & \text{otherwise}\end{cases}$ 
其中  $\kappa > 1$  为惩罚系数， $\delta_{threshold}$  为容忍阈值（典型值 0.1）。

#### 智能合约伪代码

```solidity

Contract ReputationManager:
    
    mapping(address => float) public reputationScores
    mapping(address => uint) public violationCount
    
    event ReputationUpdated(address node, float delta, string reason)
    
    function updateReputation(address node, TaskResult result):
        float delta = 0
        
        // 计算任务完成质量评分
        delta += calculateTaskScore(result)
        
        // 计算诚实性评分
        float loadDeviation = calculateLoadDeviation(node)
        if loadDeviation > DEVIATION_THRESHOLD:
            delta -= PENALTY_FACTOR * loadDeviation
            violationCount[node]++
            
            if violationCount[node] > MAX_VIOLATIONS:
                // 触发严重惩罚
                delta -= SEVERE_PENALTY
                emit BlacklistWarning(node)
        else:
            delta += HONEST_BONUS * (1 - loadDeviation)
        
        // 更新信誉分
        reputationScores[node] = 
            BASE_DECAY * reputationScores[node] + delta
        
        // 边界约束
        reputationScores[node] = 
            clamp(reputationScores[node], MIN_REP, MAX_REP)
        
        emit ReputationUpdated(node, delta, getReason())
        
        // 生成信誉更新区块
        submitReputationBlock(node, delta)
```

> 图 3.3 智能合约审计与信誉更新流程（暂时无法在飞书文档外展示此内容）
> 
> 

### 3.3.4 共识机制与延迟分析

#### 改进的 PBFT 共识

针对天基网络的特点，我们对标准 PBFT 进行以下优化：

|优化点|标准 PBFT|改进方案|效果|
|---|---|---|---|
|消息复杂度| $O(N^2)$ |分层共识| $O(N \log N)$ |
|视图切换|固定超时|自适应超时|减少无效切换|
|领导者选举|轮询|信誉加权|提高可靠性|
|检查点|固定周期|动态调整|减少开销|
**表 3.13 PBFT 共识优化对比**

#### 延迟详细建模

总共识延迟分解为：
 $D_{consensus} = D_{pre-prepare} + D_{prepare} + D_{commit} + D_{reply}$ 
各阶段延迟分布：

|阶段|分布|期望|标准差|
|---|---|---|---|
|Pre-prepare| $\mathcal{N}(\mu_{pp}, \sigma_{pp}^2)$ |50ms|15ms|
|Prepare| $\mathcal{N}(\mu_p, \sigma_p^2)$ |80ms|25ms|
|Commit| $\mathcal{N}(\mu_c, \sigma_c^2)$ |60ms|20ms|
|Reply| $Exp(\lambda_r)$ |30ms|30ms|
**表 3.14 共识各阶段延迟参数**

总延迟的期望与方差：
 $\mathbb{E}[D_{consensus}] = \mu_{pp} + \mu_p + \mu_c + \frac{1}{\lambda_r} \approx 220ms$ 
 $Var(D_{consensus}) = \sigma_{pp}^2 + \sigma_p^2 + \sigma_c^2 + \frac{1}{\lambda_r^2} \approx 1625ms^2$ 

## 3.4 多智能体强化学习算法设计

基于前述建模，我们采用多智能体近端策略优化（Multi-Agent Proximal Policy Optimization, MAPPO）算法来求解此协同资源博弈问题。

### 3.4.1 PPO 算法选择理由

选择 PPO 作为核心算法，主要基于以下考虑：

|特性|PPO 优势|对比其他算法|
|---|---|---|
|动作空间适应性|自然处理连续动作空间，通过高斯策略参数化实现平滑控制|DQN 仅适用于离散动作，需离散化导致精度损失|
|训练稳定性|裁剪目标函数限制每次更新步长，避免策略崩溃|TRPO 需要计算二阶导数，计算复杂；A3C 存在异步更新不稳定问题|
|样本效率|支持多轮更新复用同一批样本，提高数据利用率|REINFORCE 单次采样单次更新，样本效率低|
|实现成熟度|广泛应用的工业级算法，有成熟的分布式实现|SAC 等新兴算法缺乏大规模验证|
|噪声鲁棒性|对观测噪声和延迟信息具有较好容忍度|基于值的方法（如 DQN）对噪声敏感，容易产生过估计|
|超参数敏感性|超参数选择相对宽松，调参难度低|TD3 等算法对超参数非常敏感|
**表 3.15 PPO 算法选择理由对比**

### 3.4.2 训练范式：集中式训练与分散式执行 (CTDE)

我们采用集中式训练与分散式执行（Centralized Training with Decentralized Execution, CTDE）的训练范式，并创新性地引入区块链作为信息桥梁。

CTDE 范式的核心思想：

- 训练阶段：允许使用全局信息来训练一个更好的价值函数（Critic），从而获得更准确的优势估计

- 执行阶段：每个智能体仅使用本地观测来做决策，保证去中心化执行的可行性

区块链在 CTDE 中的角色：
传统 CTDE 假设训练时可以访问 "真实的" 全局状态，但在天基网络中这一假设不现实。我们的创新在于：

1. 训练时：使用区块链提供的 "延迟全局状态" 作为真实全局状态的代理

2. 执行时：智能体仅依赖本地观测和链上公共摘要

这种设计实现了对中心化信息依赖的解耦，同时保留了 CTDE 的优势。

> 图 3.4 基于区块链的 CTDE 训练框架（暂时无法在飞书文档外展示此内容）
> 
> 

### 3.4.3 网络架构设计

#### Actor 网络（策略网络）架构

|层|类型|输入维度|输出维度|激活函数|
|---|---|---|---|---|
|输入层|-| $d_l + d_g$ |-|-|
|FC1|Linear+LN| $d_l + d_g$ |256|ReLU|
|FC2|Linear+LN|256|256|ReLU|
|Attention|Multi-Head (4)|256|256|Softmax|
|FC3|Linear|256|128|ReLU|
|μ 输出层|Linear|128| $\mathcal{A}$ |-|
|σ 输出层|Linear|128| $\mathcal{A}$ |-|
**表 3.16 Actor 网络架构**

策略分布建模为对角高斯：
 $\pi_\theta(a|o) = \mathcal{N}(\mu_\theta(o), \text{diag}(\sigma_\theta(o)^2))$ 

#### Critic 网络（价值网络）架构

|层|类型|输入维度|输出维度|激活函数|
|---|---|---|---|---|
|观测嵌入|Linear| $d_o$ |128|ReLU|
|图注意力|GAT (2 层)| $N \times 128$ | $N \times 128$ |LeakyReLU|
|聚合层|Mean Pool| $N \times 128$ |128|-|
|FC1|Linear|128|64|ReLU|
|输出层|Linear|64|1|-|
**表 3.17 Critic 网络架构**

图注意力网络（GAT）用于建模智能体间的关系，注意力权重计算：
 $\alpha_{ij} = \frac{\exp(\text{LeakyReLU}(\mathbf{a}^T[\mathbf{W}h_i || \mathbf{W}h_j]))}{\sum_{k \in \mathcal{N}_i} \exp(\text{LeakyReLU}(\mathbf{a}^T[\mathbf{W}h_i || \mathbf{W}h_k]))}$ 

### 3.4.4 PPO 核心算法

#### 裁剪目标函数

设重要性采样比率：
 $\rho_t(\theta) = \frac{\pi_\theta(a_t|o_t)}{\pi_{\theta_{old}}(a_t|o_t)}$ 
PPO 的裁剪目标函数为：
 $L^{CLIP}(\theta) = \hat{\mathbb{E}}_t \left[ \min \left( \rho_t(\theta) \hat{A}_t, \text{clip}(\rho_t(\theta), 1-\epsilon, 1+\epsilon) \hat{A}_t \right) \right]$ 
其中  $\epsilon$  为裁剪系数（典型值 0.2）， $\hat{A}_t$  为优势函数估计。

裁剪机制的直观解释：

- 当  $\hat{A}_t > 0$ （好动作）时，鼓励增大  $\rho_t$ ，但最大不超过  $1+\epsilon$ 

- 当  $\hat{A}_t < 0$ （坏动作）时，鼓励减小  $\rho_t$ ，但最小不低于  $1-\epsilon$ 

#### 泛化优势估计（GAE）

 $\hat{A}_t^{GAE(\gamma, \lambda)} = \sum_{l=0}^{\infty} (\gamma \lambda)^l \delta_{t+l}$ 
其中 TD 残差：
 $\delta_t = r_t + \gamma V_\phi(s_{t+1}) - V_\phi(s_t)$ 
参数选择： $\gamma = 0.99$ ， $\lambda = 0.95$ 

#### 完整 MAPPO 算法伪代码

```Plain Text

算法1：基于区块链增强的MAPPO协同资源博弈算法

输入:
    N: 智能体数量
    M: 总训练轮次
    T: 每轮步数
    K: PPO更新迭代次数
    ε: 裁剪参数
    γ: 折扣因子
    λ: GAE参数

初始化:
    共享Actor网络参数 θ
    中心Critic网络参数 φ
    经验缓冲区 D ← ∅
    拉格朗日乘子 {λₖ} ← 初始值

For episode = 1 to M do:
    
    // ========== 数据采集阶段 ==========
    重置环境，获取初始观测 {o₁ⁱ}
    
    For t = 1 to T do:
        // 智能体决策
        For i = 1 to N do:
            从区块链查询公共摘要 gₜ
            构建观测 oₜⁱ ← (lₜⁱ, gₜ)
            采样动作 aₜⁱ ~ πθ(·|oₜⁱ)
        End For
        
        // 环境交互
        执行联合动作 aₜ = (aₜ¹, ..., aₜᴺ)
        观测奖励 {rₜⁱ} 和下一状态 {oₜ₊₁ⁱ}
        
        // 区块链状态更新
        提交 Resource/Task 区块到联盟链
        
        // 存储经验
        D ← D ∪ {(oₜ, aₜ, rₜ, oₜ₊₁)}
    End For
    
    // ========== 优势估计 ==========
    For i = 1 to N do:
        使用 Vφ 计算GAE优势 {Âₜⁱ}
        计算回报目标 Rₜⁱ = Âₜⁱ + Vφ(sₜ)
    End For
    
    // ========== PPO策略更新 ==========
    For k = 1 to K do:
        从D中采样mini-batch B
        
        // 计算各损失项
        L_clip ← 裁剪代理目标
        L_vf ← 价值函数损失: ||Vφ(s) - R||²
        L_ent ← 策略熵正则: -H(πθ)
        
        // 约束惩罚项
        For each constraint c do:
            violation_c ← max(0, gₖ(θ))
            L_constraint ← L_constraint + λₖ · violation_c
        End For
        
        // 总损失
        L_total ← L_clip - c₁·L_vf + c₂·L_ent - L_constraint
        
        // 梯度更新
        θ ← θ + αₐ · ∇θL_clip
        φ ← φ - αc · ∇φL_vf
        
        // 对偶更新拉格朗日乘子
        For each constraint c do:
            λₖ ← [λₖ + αλ · violation_c]⁺
        End For
    End For
    
    // 清空缓冲区
    D ← ∅
    
    // 周期性同步区块链策略参数
    If episode mod sync_interval == 0 then:
        提交 Policy-Sync 区块到联盟链
    End If

End For

输出: 训练好的策略网络 πθ*
```

### 3.4.5 参数自适应调整机制

为提升算法在不同场景下的适应性，我们引入以下自适应机制：

- 学习率线性衰减：
 $\alpha_t = \alpha_0 \cdot \max\left( 1 - \frac{t}{T_{total}}, \alpha_{min} \right)$ 
典型参数： $\alpha_0 = 3 \times 10^{-4}$ ， $\alpha_{min} = 10^{-5}$ 

- 裁剪系数自适应：
 $\epsilon_t = \epsilon_0 \cdot \left( 1 + \beta \cdot \frac{KL(\pi_\theta || \pi_{\theta_{old}})}{KL_{target}} \right)^{-1}$ 
当 KL 散度过大时，自动减小裁剪系数以获得更保守的更新。

- 公平权重动态调整：
 $w_f(t) = w_f^{base} + \Delta w \cdot \sigma\left( \frac{Var(L_t) - Var_{target}}{Var_{scale}} \right)$ 
当负载方差高于目标时，自动增加公平权重。

## 3.5 仿真实验与性能评估

为全面、严谨地验证本章所提 "基于区块链与 MARL/PPO 的系统层协同资源博弈方法" 的有效性、鲁棒性与可扩展性，我们设计并实现了一个高保真的离散事件仿真平台。

### 3.5.1 实验设计总览

#### 仿真环境与数据生成

本研究的所有实验数据均通过合成仿真生成，遵循 "任务生成→状态观测→策略决策→环境反馈→状态更新" 的闭环流程。为保证实验的可复现性，所有随机过程均由固定的主随机种子控制。

#### 关键仿真参数

|参数类别|参数名称|取值 / 范围|描述|
|---|---|---|---|
|网络配置|节点规模 N|{10, 20, 50, 100}|模拟不同规模网络|
||节点异构性|高 / 中 / 低 三档|算力变异系数|
||网络连通度|p ∈ [0.3, 0.8]|随机图 G (N,p)|
|任务模型|到达率 λ|与并发度因子成正比|Poisson 过程|
||并发度因子|[0.5, 2.5]|控制系统负载|
||任务类型分布|(0.5, 0.35, 0.15)|轻 / 中 / 重型|
|联盟链|共识延迟|N(220, 40²) ms|PBFT 延迟|
||区块周期|2 时间步|出块间隔|
||异常率|[0, 0.15]|虚报概率|
|MAPPO|折扣因子 γ|0.99|-|
||GAE 参数 λ|0.95|-|
||裁剪系数 ε|0.2|-|
||训练轮次|500|-|
||每轮步数|200|-|
||Actor 学习率|3×10⁻⁴|-|
||Critic 学习率|1×10⁻³|-|
||PPO 迭代次数 K|10|-|
||Mini-batch|64|-|
**表 3.18 关键仿真参数设置**

#### 对比基线方法

|方法|类型|描述|
|---|---|---|
|Selfish|去中心化贪婪|每个节点最大化自身即时收益|
|Round-robin|无状态公平|轮询分配，不感知状态|
|Heuristic|负载感知启发式|分配给负载最低的节点|
|Random|随机基线|均匀随机选择目标节点|
|Single-PPO|单智能体 RL|忽略多智能体交互|
|Centralized|理论上界|假设全局实时信息可用|
**表 3.19 对比基线方法**

#### 评价指标体系

|维度|指标|计算方式|目标|||
|---|---|---|---|---|---|
|公平性|Jain 指数| $J = \frac{(\sum L_i)^2}{N\sum L_i^2}$ |↑ 越大越好|||
||Gini 系数|\ $\$ G = \frac{\sum\sum|L_i-L_j|}{2N \sum L_i}\ $\$ |↓ 越小越好|
||负载方差| $Var(L)$ |↓ 越小越好|||
|效率|吞吐量|tasks/step|↑ 越大越好|||
||平均时延|steps|↓ 越小越好|||
||95 分位时延|steps|↓ 越小越好|||
||任务成功率|%|↑ 越大越好|||
|收敛性|收敛轮次|episodes|↓ 越快越好|||
||最终回报|-|↑ 越高越好|||
**表 3.20 评价指标体系**

### 3.5.2 算法收敛性分析

#### 训练回报收敛曲线

图 3.5 展示了在中等规模网络（N=20）和中等负载条件下的训练收敛曲线。

> 图 3.5 训练回报收敛曲线 (N=20, 中等负载)（暂时无法在飞书文档外展示此内容）
> 
> 

|方法|收敛轮次|最终回报|回报标准差|
|---|---|---|---|
|MAPPO|250±20|450±15|12.3|
|Single-PPO|350±30|300±20|18.5|
|Heuristic|-|85±5|4.2|
**表 3.21 收敛性能对比 (5 次独立实验)**

关键观察：

1. MAPPO 在约 250 轮后达到稳定收敛，收敛速度优于 Single-PPO（约 350 轮）

2. 最终累积回报 MAPPO（\450）显著高于 Single-PPO（\300）

3. 训练曲线波动较小，表明 PPO 裁剪机制有效控制了更新幅度

#### 奖励分量演化

|训练阶段|自利项占比|公平项占比|稳定项占比|主导因素|
|---|---|---|---|---|
|初期 (0-100 轮)|65%|25%|10%|自利探索|
|中期 (100-250 轮)|45%|45%|10%|公平 - 效率平衡|
|后期 (250-500 轮)|35%|55%|10%|协同主导|
**表 3.22 奖励分量演化统计**

这一演化模式揭示了智能体从 "自利探索" 到 "协同共赢" 的学习转变过程。

### 3.5.3 综合性能对比分析

#### 负载均衡分析

|方法|并发度 0.5|并发度 1.0|并发度 1.5|并发度 2.0|并发度 2.5|
|---|---|---|---|---|---|
|Selfish|0.35±0.08|0.58±0.12|0.85±0.15|1.25±0.22|1.78±0.31|
|Round-robin|0.18±0.04|0.28±0.06|0.42±0.08|0.58±0.11|0.76±0.14|
|Heuristic|0.15±0.03|0.24±0.05|0.35±0.07|0.48±0.09|0.62±0.12|
|Single-PPO|0.12±0.03|0.19±0.04|0.28±0.06|0.38±0.08|0.49±0.10|
|MAPPO|0.06±0.02|0.09±0.02|0.12±0.03|0.16±0.04|0.21±0.05|
|Centralized|0.04±0.01|0.06±0.02|0.08±0.02|0.11±0.03|0.15±0.04|
**表 3.23 负载方差对比 (N=20)**

#### Jain 公平指数演化

|训练轮次|MAPPO|Single-PPO|Heuristic|Selfish|
|---|---|---|---|---|
|0|0.58±0.08|0.55±0.09|0.72±0.05|0.52±0.10|
|100|0.75±0.06|0.68±0.07|0.74±0.05|0.54±0.11|
|200|0.88±0.04|0.78±0.05|0.75±0.05|0.55±0.10|
|300|0.93±0.02|0.82±0.04|0.76±0.05|0.56±0.10|
|400|0.94±0.02|0.84±0.04|0.76±0.05|0.57±0.09|
|500|0.95±0.02|0.85±0.03|0.76±0.05|0.58±0.09|
**表 3.24 Jain 公平指数演化 (N=20, 并发度 1.5)**

#### 系统效率指标

|方法|吞吐量|平均时延|95 分位时延|任务成功率|
|---|---|---|---|---|
|Selfish|1.8±0.3|8.5±2.1|15.2±4.3|0.78±0.08|
|Round-robin|2.5±0.2|5.1±1.5|10.8±2.8|0.88±0.05|
|Heuristic|2.8±0.2|4.2±1.1|8.5±2.2|0.91±0.04|
|Single-PPO|3.0±0.2|3.8±0.9|7.2±1.8|0.93±0.03|
|MAPPO|3.5±0.1|2.8±0.8|5.5±1.4|0.96±0.02|
|Centralized|3.8±0.1|2.4±0.6|4.8±1.2|0.98±0.01|
**表 3.25 系统效率指标对比 (N=20, 并发度 1.5)**

#### 时延 CDF 分析

|时延阈值|MAPPO|Heuristic|Selfish|
|---|---|---|---|
|≤2 steps|35%|22%|12%|
|≤4 steps|72%|55%|35%|
|≤6 steps|92%|78%|55%|
|≤8 steps|98%|90%|72%|
|≤10 steps|99.5%|96%|85%|
**表 3.26 任务完成时延 CDF 分布**

### 3.5.4 消融研究

#### 消融实验设计

|变体|修改内容|目的|
|---|---|---|
|no_chain|移除区块链公共摘要|验证链上信息必要性|
|no_fairness|移除公平项奖励|验证公平性引导作用|
|no_stability|移除稳定项惩罚|验证稳定约束效果|
|no_CTDE|改为独立学习|验证 CTDE 范式优势|
|fixed_weights|固定奖励权重|验证自适应权重效果|
**表 3.27 消融实验设计**

#### 消融实验结果

> 图 3.6 消融实验雷达图对比（暂时无法在飞书文档外展示此内容）
> 
> 

| 变体 | Jain 指数 | 负载方差 | 吞吐量 | 平均时延 | 收敛轮次 |
> （注：文档部分内容可能由 AI 生成）