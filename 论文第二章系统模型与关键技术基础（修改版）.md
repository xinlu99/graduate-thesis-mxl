# 论文第二章系统模型与关键技术基础（修改版）

本章旨在为后续章节中提出的分布式智能管控方法构建统一的系统模型、任务模型与问题描述。我们首先对天基网络的系统架构进行抽象，定义其中包含的关键实体、交互关系与核心挑战。

## 2.1 系统架构与挑战

### 2.1.1 系统架构

本文所研究的天基网络系统是一个由大量异构节点构成的分布式网络，其核心目标是在资源受限和高动态环境下，为各类天基试验任务提供高效、可靠的计算服务。如图 2.1 所示，该系统主要由三类实体构成：

1. 卫星终端 (Satellite Terminal, ST)：ST 是任务的产生源，通常是搭载了传感器或试验载荷的低轨（LEO）卫星。它们负责执行数据采集、在轨科学探测等任务，并产生具有计算需求的数据。ST 自身的计算能力和能量储备非常有限，难以独立处理所有计算密集型任务，因此构成了计算卸载的发起方。

2. 协同计算节点 (Cooperative Computing Node)：这类节点是任务的潜在承载方，为 ST 提供计算卸载服务。根据部署位置和能力的不同，可以细分为：

- 卫星边缘节点 (Satellite Edge Node, SEN)：SEN 是指具备较强计算和存储能力的卫星，例如拥有专用计算载荷或资源较为充裕的通信卫星。它们通过星间链路（Inter-Satellite Link, ISL）与 ST 通信，能够提供低时延的协同计算服务。

- 地面网关 (Ground Gateway)：部署在地面，通过星地链路（Satellite-to-Ground Link, SGL）与卫星连接。地面网关拥有近乎无限的计算资源，但受限于卫星过顶时间窗口和较长的传播时延，通常用于处理非时延敏感或计算量极大的任务。

1. 星座控制中心 (Constellation Controller, CC)：CC 是系统的地面管控实体，但不直接参与实时任务调度。在本框架中，CC 承担着系统级的 “后台” 管理职能，包括：作为联邦学习的服务器，聚合各 SEN 上传的模型更新，训练全局的并发开销预测模型；作为多智能体强化学习的中心化训练模块，在离线阶段辅助各节点策略的学习与演化；以及负责联盟链网络的治理，如版本更新、节点准入和激励机制的发布。
此外，联盟链网络作为本框架的核心基础设施，贯穿于各实体之间。它并非一个物理实体，而是一个由所有 ST 和 SEN 共同维护的分布式账本，用于记录全网的任务状态和资源快照，为去中心化环境下的可信协同提供数据基础。

### 2.1.2 链路模型与符号定义

系统中的通信主要通过星间链路和星地链路完成。链路的特性对任务卸载的时延和成功率有决定性影响。

- 星间链路 (ISL)：带宽相对受限且存在时延，其连通性随卫星相对运动而动态变化。

- 星地链路 (SGL)：带宽较高，但受限于可见时间窗口，且传播时延显著。
为了后续建模的清晰性，我们定义以下关键符号集合：

- 所有节点的集合表示为  $\mathcal{V} = \mathcal{V}_{ST} \cup \mathcal{V}_{SEN}$ ，其中  $\mathcal{V}_{ST}$  是 ST 节点集合， $\mathcal{V}_{SEN}$  是 SEN 节点集合。

- 时间被离散化为一系列时间片  $\mathcal{K} = {1, 2, ..., K}$ 。

- 在时间片  $k \in \mathcal{K}$ ，从节点  $i$  到节点  $j$  的链路传输速率表示为  $R_{i,j}(k)$ 。

### 2.1.3 核心挑战

基于上述架构，天基网络在执行计算密集型任务时面临四大核心挑战：

1. 高动态性 (High Dynamics)：卫星节点的快速移动导致网络拓扑结构、节点间的链路质量（带宽、时延）以及节点的可用能量随时间剧烈变化。这要求卸载和调度策略必须具备高度的自适应能力。

2. 信息不完全 (Partial Observability)：在去中心化环境下，任何单个节点都难以实时获取整个网络的全局状态信息（如所有节点的瞬时负载、队列长度和链路质量）。节点决策往往基于延迟、不完整和带有噪声的局部观测，这对决策的鲁棒性提出了极高要求。

3. 并发影响 (Concurrency Effect)：当多个任务在同一计算节点上并发执行时，它们会竞争 CPU、内存、I/O 等资源，导致每个任务的实际执行时间均被延长。这种由并发引起的新增开销被称为并发开销，它具有非线性和难以预测的特点，是影响任务完成时延的关键因素。

4. 信任缺失 (Lack of Trust)：在缺乏中心化协调和可信约束机制时，作为独立决策主体的各计算节点可能优先考虑自身利益（如最小化自身能耗），做出 “自利” 决策。这种行为可能导致局部最优决策的叠加，最终损害系统整体性能，例如造成某些节点的 “公地悲剧” 式拥塞。

## 2.2 区块链可信协同基础

为应对 “信息不完全” 和 “信任缺失” 两大挑战，本文引入区块链技术构建一个去中心化的可信公共状态共享机制。

### 2.2.1 为何选择联盟链

公有链（如比特币、以太坊）虽然实现了完全的去中心化和开放性，但其工作量证明（PoW）等共识机制需要消耗大量计算资源，且交易确认时延较长，不适用于天基网络这种资源受限、对时延有一定要求的场景。相比之下，联盟链（Consortium Blockchain）在性能、安全和隐私之间取得了更好的平衡。它由一组预先授权的节点（本文中为 ST、SEN 和 CC）共同维护，采用如实用拜占庭容错（PBFT）等更高效的共识算法，能够在保证数据一致性和不可篡改性的同时，提供更低的交易时延和更高的吞吐量，且能耗更低。

结合天基网络的特点，需要针对不同场景优选共识机制：拜占庭容错算法（如 PBFT）容错度高，但节点数量较多时性能较差、通信开销大；Raft 算法实现简单、易于部署，但存在领导者节点单点故障风险；PoA 适用于对性能要求较高的私有链场景，但安全性依赖节点可信度；DPoS 适用于大规模分布式系统，但存在代表节点集中化风险。在天基网络试验智能管控系统中，可根据节点数量动态调整共识机制，例如节点数量较少时采用 Raft 算法，节点数量较多时采用 DPoS 算法，以在性能和容错性之间取得平衡。

### 2.2.2 核心机制与作用

在本框架中，联盟链扮演着 “公共信息板” 的角色，如图 2.2 所示，其关键机制服务于上层智能决策：

- 智能合约 (Smart Contract)：预先定义的、可自动执行的链上代码。我们利用智能合约来规范任务的生命周期管理、资源状态的更新逻辑以及对不规范行为（如超时未提交任务结果）的惩罚规则。

- 链上 / 链下协同存储：考虑到星上存储资源有限，我们将关键的状态摘要和哈希指针记录在链上（On-chain），而将详细的原始数据或任务结果存储在链下（Off-chain）的分布式存储系统中（如 IPFS 或节点的本地存储）。这种方式既保证了数据的可追溯性和完整性，又避免了区块链的膨胀问题。

- 状态查询与非实时全局态势：任何联盟成员都可以查询链上数据，从而获得关于全网资源和任务历史的、一致且不可篡改的视图。尽管由于共识和传播延迟，这个视图是非实时的，但它为每个节点提供了宝贵的 “非实时全局态势”，远优于纯粹依赖局部观测。

同时，本框架采用分层多域联盟链架构，包括主链、子链和跨链网关。主链负责存储全局的核心状态和子链的哈希摘要，子链负责各类区域、部门或具体任务的数据管理，跨链网关实现主链与子链之间的数据交换。这种架构有效提高了任务数据管理的可靠性、拓展性和访问性能，适配了天基网络多任务、多区域的分布式管控需求。

### 2.2.3 链上记录字段与开销模型

为了支撑系统层和任务层的决策，我们在联盟链上设计了三类核心的记录块（以交易形式提交）：

|块类型|核心字段|作用与目的|
|---|---|---|
|资源块 (Resource Block)|节点 ID、时间戳、CPU 利用率、内存使用、可用能量、当前排队任务数|SEN 节点周期性地上报自身资源状态，形成可追溯的资源快照历史。|
|任务开始块 (Task Start Block)|任务 ID、属主 ST、目标 SEN、任务数据大小、预计计算量、提交时间戳|在 ST 决定卸载任务至 SEN 时生成，标志任务生命周期的开始。|
|任务结束块 (Task End Block)|任务 ID、执行结果哈希、开始执行时间、结束执行时间、实际资源消耗、执行状态（成功 / 失败）|在 SEN 完成任务后生成，记录任务执行的真实性能数据，用于后续的并发开销预测、信誉评估和激励计算。|
表 2.1 链上记录字段设计

将信息上链会引入额外的时延开销。一个完整的上链操作包含交易广播、多节点共识和区块确认等步骤。我们可以将其简化建模为一个链上确认延迟  $D_{BC}$ ，它主要由网络传播时延  $D_{prop}$  和共识时延  $D_{cons}$  构成：
 $D_{BC} = D_{prop} + D_{cons}$ 
这个延迟是后续评估系统总时延时需要考虑的重要部分。

此外，为了实现主链与子链的数据一致性，业务子链的数据需要定期或实时存储到主链上。假设业务子链中的区块序列为 ${{{B}{{{}{}{}{}}}}_{{s1}{{{}{}{}{}}}}{,}{{B}{{{}{}{}{}}}}_{{s2}{{{}{}{}{}}}}{,}{{B}{{{}{}{}{}}}}_{{s3}{{{}{}{}{}}}}{,…,}{{B}{{{}{}{}{}}}}_{{sm}{{{}{}{}{}}}}$ ，主链中的区块序列为 ${{{B}{{{}{}{}{}}}}_{{m1}{{{}{}{}{}}}}{,}{{B}{{{}{}{}{}}}}_{{m2}{{{}{}{}{}}}}{,}{{B}{{{}{}{}{}}}}_{{m3}{{{}{}{}{}}}}{,…,}{{B}{{{}{}{}{}}}}_{{mn}{{{}{}{}{}}}}$ 。每个业务子链区块 ${{{B}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}$ 包含数据 ${{{D}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}}$ 、前一个区块的哈希值 ${{H}{(}{{B}{{{}{}{}{}}}}_{{si−1}{{{}{}{}{}}}}{)}}$ 、当前区块的哈希值 ${{H}{(}{{B}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{)}}$ ，其存储过程满足：
 ${\begin{eqnarray}{{&}{{B}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{={}{{D}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{,}{H}{(}{{B}{{{}{}{}{}}}}_{{si−1}{{{}{}{}{}}}}{),}{H}{(}{{B}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{)}}{{{}{}}}}\{{&}{H}{(}{{B}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{)=}{H}{(}{{D}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{∥}{H}{(}{{B}{{{}{}{}{}}}}_{{si−1}{{{}{}{}{}}}}{))}{{{}{}}}}\\\end{eqnarray}}$ 
定期将业务子链区块的哈希值 ${{H}{(}{{B}{{{}{}{}{}}}}_{{si}{{{}{}{}{}}}}{)}}$ 转移到主链的区块 ${{{B}{{{}{}{}{}}}}_{{mi}{{{}{}{}{}}}}}$ 中，以确保业务子链的数据在主链上有可靠的记录，数据转移过程满足：
> （注：文档部分内容可能由 AI 生成）